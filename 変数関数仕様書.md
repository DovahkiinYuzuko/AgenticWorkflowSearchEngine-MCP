# 変数・関数仕様書 (改訂版)

本仕様書は、Agentic Workflow Search Engine MCP & CLI における、新規追加・変更された主要な変数および関数と、それらの依存関係・影響範囲をマッピングしたものです。
コンテキスト上限を超えた長文を安全に処理する「動的チャンク要約（MapReduce）」および、AIの推論精度を最大化する「最終要約・ファクトチェックの別セッション分離」を網羅しています。

---

## 1. 新規追加された関数

### 1.1. `academicSearch(keywords, limitInput)`
- **定義場所**: [academic-search.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/academic-search.js)
- **概要**: arXiv API および PubMed API を用いて、ブラウザによる巡回（Playwrightスクレイピング）を行わずに学術論文情報を直接かつサイトに優しく取得する。
- **引数**:
  - `keywords` (string): 検索キーワード
  - `limitInput` (number): 取得上限件数
- **戻り値**: `Promise<Array<Object>>` (既存パイプラインの `phase1Results` と互換性のあるオブジェクトの配列)
- **依存関係 (Dependencies)**:
  - `fetch` (Node.js グローバル関数)
  - `src/utils/sanitize.js`
  - `fs` (ファイル書き込み用)
- **影響範囲 (Impact Scope)**:
  - `runPipeline` で学術モード (`mode: 'academic'`) が選択された際の処理フロー。

### 1.2. `cliLogger.isMcp()`
- **定義場所**: [cli-logger.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/utils/cli-logger.js)
- **概要**: 現在のアプリケーションがMCPサーバーモード（サイレントモード）として起動されているかを取得する。
- **引数**: なし
- **戻り値**: `boolean` (`isSilent` フラグの値)
- **依存関係 (Dependencies)**: なし
- **影響範囲 (Impact Scope)**:
  - MCPサーバー接続時における自律調査の `interactive` モード（キー入力待ちデッドロック）を防止するためのフォールバック処理。

### 1.3. `callOllamaStreaming(prompt, url, intent, onChunk)`
- **定義場所**: [4-structure.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/4-structure.js) 及び [5-finalize.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/5-finalize.js)
- **概要**: Ollama APIを呼び出し、ストリーミングでトークンを受信しながら結果を累積しつつ、進捗をリアルタイムにリレー/表示する共通ストリーミング・ヘルパー。
- **引数**:
  - `prompt` (string): AIへの命令文
  - `url` (string): 出典参照用URL (5-finalizeでは非使用)
  - `intent` (string): 検索意図 (5-finalizeでは非使用)
  - `onChunk` (function): 各トークン受信時のコールバック関数
- **戻り値**: `Promise<string>` (最終的なテキスト全体)
- **依存関係 (Dependencies)**:
  - `config` (`config.ollama` 設定情報)
  - `fetch` (Ollama API 呼び出し)
  - `AbortController` (タイムアウト管理用)
- **影響範囲 (Impact Scope)**:
  - 単一ページの構造化、MapReduceによる部分要約・統合要約、最終レポート（ファクトチェック＆要約）生成のすべて。

### 1.4. `generateFactCheck(validSummaries, intent, locale)`
- **定義場所**: [5-finalize.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/5-finalize.js)
- **概要**: 各情報ソースの要約データを比較し、合意事項、矛盾・対立点、および商業バイアスや根拠不足の主張を抽出・分類する「セッション1：ファクトチェック」を実行する。
- **引数**:
  - `validSummaries` (string): 各ページのAI要約リスト
  - `intent` (string): 元の検索意図
  - `locale` (string): システムロケール
- **戻り値**: `Promise<string>` (ファクトチェックセクションのMarkdown)
- **依存関係 (Dependencies)**:
  - `callOllamaStreaming`
- **影響範囲 (Impact Scope)**:
  - `generateFinalSummary` 内でのファクトチェック生成部。

### 1.5. `generateComprehensiveSummary(validSummaries, intent, locale, csvIndexContent)`
- **定義場所**: [5-finalize.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/5-finalize.js)
- **概要**: 各情報ソースから重複を排除し、検索意図に対して極めてロジカルで構造的な答えを抽出・記述する「セッション2：サマリー合成」を実行する。
- **引数**:
  - `validSummaries` (string): 各ページのAI要約リスト
  - `intent` (string): 元の検索意図
  - `locale` (string): システムロケール
  - `csvIndexContent` (string): (追加) 生成された `index.csv` の生データ
- **戻り値**: `Promise<string>` (総合サマリーセクションのMarkdown)
- **依存関係 (Dependencies)**:
  - `callOllamaStreaming`
- **影響範囲 (Impact Scope)**:
  - `generateFinalSummary` 内でのサマリー合成生成部。プロンプトへの指示により、回答の末尾に物理ピン打ち逆引き参照テーブル（Fact-Checking用テーブル）が生成される。

---

## 2. 変更された既存の関数

### 2.1. `markdownToJson(mdContent, mdPath, url, title, jsonPath, intent)`
- **定義場所**: [4-structure.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/4-structure.js)
- **変更概要**: 入力テキストを `# 見出し` 単位の論理境界で最大8,000文字ずつの複数チャンクに自動で動的分割し、各チャンクの「部分要約（Map）」とそれらの「再要約・統合（Reduce）」を順次ストリーミング実行するMapReduceアルゴリズムへ移行。
  **【追加変更】**: セクションごとの開始行・終了行を特定し、Ollamaで抽出したトピックタグとともに、物理的なピン打ち参照インデックスとして `index.csv` へ一元的に追記保存する機能を統合。
- **引数**: 変更なし
- **戻り値**: 変更なし（JSONデータ）
- **依存関係 (Dependencies)**:
  - `callOllamaStreaming` (新規依存)
- **影響範囲 (Impact Scope)**:
  - 単一ページの構造化リファイン処理。入力文字数無制限（-1）設定時の、長文ドキュメントに対するローカルLLMコンテキスト溢れ防止。

### 2.2. `generateFinalSummary(results, intent)`
- **定義場所**: [5-finalize.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/5-finalize.js)
- **変更概要**: AIのタスク負荷を最小化して精度を高めるため、Ollama呼び出しを「ファクトチェック専用セッション」と「総合要約専用セッション」の2つの独立プロセス（セッション）に分離し、最終的にそれらの成果物を改行コードで単純連結（Join）して1つの完成レポートにする設計へ変更。
- **引数**: 変更なし
- **戻り値**: 変更なし（Markdownテキスト）
- **依存関係 (Dependencies)**:
  - `generateFactCheck` (新規依存)
  - `generateComprehensiveSummary` (新規依存)
- **影響範囲 (Impact Scope)**:
  - `runPipeline` で生成される最終レポート（`_summary.md`）のフォーマットと分析の品質。

### 2.3. `runPipeline(keywords, intent, limitInput, enableFinalSummary, mode, deepDive)`
- **定義場所**: [index.js (Runner)](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/index.js)
- **変更概要**: 引数に `mode` と `deepDive` を追加し、学術モードのディスパッチおよび自律的二段階検索を実装。さらに、MCPサーバーモード実行時に `deepDive` が `interactive`（対話型キー入力待ち）に指定された場合、`stdin` のデッドロックを防ぐため自動的に `auto` へフォールバックする resolvedDeepDive ガードを導入。
- **依存関係 (Dependencies)**:
  - `academicSearch` (新規依存)
  - `generateDeepDiveQuery` (新規依存)
  - `cliLogger.isMcp` (新規依存)
- **影響範囲 (Impact Scope)**:
  - アプリケーション全体のメインオーケストレーションおよびMCP/CLIでの安定動作。

### 2.6. `broadcast(data)`
- **定義場所**: [streaming-server.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/utils/streaming-server.js)
- **変更概要**: WebSocket で配信されるデータをプレーンテキストから `{ type, value }` 形式の構造化されたJSONオブジェクト送信へ刷新。プレーンテキストが渡された場合も自動で `{ type: 'token', value: data }` にラッピングする後方互換処理を追加。
- **引数**:
  - `data` (any): 送信するオブジェクトまたはレシーバー互換文字列
- **戻り値**: なし
- **依存関係 (Dependencies)**: なし
- **影響範囲 (Impact Scope)**:
  - `ollama-viewer.js` への進捗情報のリアルタイム配信プロトコル全体。

### 2.7. `ws.on('message')` メッセージ受信ハンドラ
- **定義場所**: [ollama-viewer.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/utils/ollama-viewer.js)
- **変更概要**: WebSocketからの受信メッセージパース処理について、意図的な `SyntaxError` スローに依存するフローを撤廃。`type` プロパティ（`token`, `info`, `control`）をスマートに評価してバッファやUIを描画する設計に改修。レシーバー互換およびプレーンテキスト受信時のフォールバックも維持。

### 2.4. `getCacheSummaryPath(cacheKey)`
- **定義場所**: [cache.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/utils/cache.js)
- **変更概要**: `cacheKey` (書式 `mode:keywords`) を受け取り、モード名とキーワードに分解して、保存ディレクトリをプレフィックスなしの `artifacts/<keywords>/` に、ファイル名を `summary.md` (webモード) および `summary_<mode>.md` (academicモード等) にマッピングする。
- **引数**:
  - `cacheKey` (string): キャッシュを識別する一意のキー（`mode:keywords`）
- **戻り値**: `string` (サマリーMarkdownファイルの絶対パス)
- **依存関係 (Dependencies)**:
  - `sanitizeFolderName` (`src/utils/sanitize.js`)
- **影響範囲 (Impact Scope)**:
  - `checkCache` および `saveCache` が使用するすべてのサマリーキャッシュ保存パス決定処理。

### 2.5. `isSameArticleUrl(urlA, urlB)`
- **定義場所**: [2-capture.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/pipeline/2-capture.js)
- **変更概要**: 同一カテゴリの別ID個別記事をページネーションと誤認しないように「数値のみで値が異なるセグメントの排除」および「ページ番号パターンの空文字チェック」、サブパス遷移時の「元パスとの完全一致検証」を導入し、パス類似度の判定を厳格化。さらに、WindowsのMAX_PATH（260文字）制限対策としてタイトル文字数を最大50文字に安全切り詰めする処理を統合。
- **引数**:
  - `urlA` (string): 比較元（1ページ目）のURL
  - `urlB` (string): 比較対象（遷移候補）のURL
- **戻り値**: `boolean` (同一記事の分割ページである可能性が高い場合は `true`)
- **依存関係 (Dependencies)**: なし
- **影響範囲 (Impact Scope)**:
  - `detectNextPageLink` で巡回するパジネーション先の判定精度。無関係な別記事へのジャンプの完全シャットアウト。


---

## 3. 引数マッピングおよび設定の拡張

### 3.1. CLI引数およびフラグの追加
- **対象ファイル**: [cli.js](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/cli.js)
- **新規追加フラグ**:
  - `--mode <web|academic>`: 検索ソースの切り替え
  - `--deep-dive <auto|interactive|none>`: 二段階検索の制御方式の切り替え

### 3.2. MCPツールプロパティおよびバリデーションの拡張
- **対象ファイル**: [index.js (Root)](file:///c:/Users/rikui/Documents/VSCode/AgenticWorkflowSearchEngine-MCP/src/index.js)
- **新規パラメータおよび制約**:
  - `mode` (string): デフォルト `"web"`。利用可能な値を制限するため `enum: ["web", "academic"]` を定義。
  - `deep_dive` (string): デフォルト `"auto"`。利用可能な値を制限するため `enum: ["auto", "none"]` を定義（MCPデッドロック回避のため `interactive` は除外）。
- **バリデーションガードの追加**:
  - `CallToolRequestSchema` 受信ハンドラ部に、必須引数である `keywords` および `intent` の早期存在チェックバリデーションを追加。パラメータ不足時は親AIに `isError: true` を含む明快なエラーレスポンスを返すようにし、クラッシュを防止。
- **MCP Resources機能の追加**:
  - `ListResourcesRequestSchema`: `artifacts/` 配下に保存されている抽出済み `.md` や `.csv` ファイルを再帰的に探索し、AIが直接読み込めるリソース (`file:///...`) として一覧公開するハンドラを追加。
  - `ReadResourceRequestSchema`: 指定されたリソースURI（ローカルファイルパス）からテキストを非同期で読み出し、プレーンテキストとしてAIに直接返却するハンドラを追加。
