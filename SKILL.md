# Skill: Agentic Workflow Search Engine

## Overview
このスキルは、AIエージェントが `aw-se-mcp` (Agentic Workflow Search Engine MCP) を効果的に活用して、ユーザーに代わって高度な自律的リサーチを行うためのガイドラインです。

## Capabilities
MCPサーバーを通じて提供される `search_and_extract` ツールを使用することで、単純なキーワード検索ではなく、ページクロール、マークダウン抽出、JSON構造化、および二次的な深掘り検索（Deep Dive）までを一気通貫で実行できます。

## Instructions
AIエージェントとして、以下のルールに従ってツールを呼び出してください。

### 1. ツールの使い分け
- **一般的な調査（Web検索）**: `mode: "web"` (デフォルト) を使用してください。Playwrightをバックグラウンドで起動し、動的なWebページもクロールします。
- **学術的・医療的・専門的な調査**: `mode: "academic"` を明示的に指定してください。arXivやPubMedなどのAPIを使用し、スクレイピング不要で論文を検索します。

### 2. クエリの最適化
- **keywords**: ユーザーの質問をそのまま投げるのではなく、検索エンジンが理解しやすいキーワード（複数単語）に分解・最適化して指定してください。
- **intent**: 最も重要なパラメータです。「誰の何を知りたいのか」「どのようなデータ形式で抽出してほしいのか」を具体的に指示してください。（例: "最新のリリース日と主な新機能を抽出して"）

### 3. 深掘りと要約
- **Deep Dive**: 複雑なトピックや、一度の検索では情報が網羅できないと予想される場合は、`deep_dive: "auto"` を指定してください。ツール側で自動的に二次検索を実行します。
- **Global Synthesis**: 複数の検索結果を統合した最終的な結論が必要な場合は、`final_summary: true` に設定してください。

### 4. 成果物の取得
- 検索完了後、詳細なデータや生成されたマークダウンファイルを確認する必要がある場合は、MCPのリソース（`mcp://artifacts/...`）から取得して内容を読み取ってからユーザーに回答してください。
